# Real CE29 production export: fixed published joint-score application

User request on 2026-09-13: check the latest GitHub joint indicator and apply it to the existing real CE29-adapted-library deconvolution, reporting how many identities remain. The published experiment branch head was checked as 704e05fa35e39b15735fd5c600eaed9f8510db0f. GitHub master is the earlier V59 implementation f7d6e47; it is not the latest joint-score branch. Use the exact published monotone five-term model and its original DEV thresholds; no model fit or real-data threshold selection.

Read only the known production export `decon-lipid/results_758_v38_ce29_empiricalfwhm_globalq99_fixed_library_joint_earlystop`, the directly associated V38 ISTA-ready assets including its own foreground mask, the frozen production lock, existing physical components/block manifest, and the published score model/helpers. Keep production X exactly unchanged. Use exported A_calibrated as the A associated with X, recording its small float32 normalization differences from the CPU benchmark A; require identical support/candidate ordering and max difference <=4 float32 eps. Do not renormalize any matrix, observation, pixel or abundance.

Aggregate the original foreground candidate means with gate >0.001. Molecular identity is exact lipid_name, with sum of reported-alias abundance and max reported-alias original rho. Compute original rho on the mean measured foreground spectrum where it is absent. Although the published corrected model has a zero rho coefficient, retain real computed rho rather than inventing placeholders.

Reuse the existing 34 physical blocks, all 391 competitor columns and all 377 molecular-identity deletions. Compute full/delete held-block predictions with the unchanged NNLS primitive and KKT checks. Reuse the exact existing predictive_gain and positive_block_fraction formulas, DEV normalization, scoring function and complete-tie threshold semantics. These are necessary feature computations for this new observation; no full-image deconvolution, training, raw-MSI processing, simulation, pool refit or GPU is executed. Four local CPU workers with one BLAS thread each; isolated NumPy2.1.3/SciPy1.15.3 runtime, no experimental data upload.

Report all three existing cuts: high-recall development pool 0.6299285665027182; DEV5% cut 0.8050711504567813; DEV1% cut 0.958782322096608. These names identify synthetic development policies, not measured FDR or recall on the real sample. Real molecular truth is unknown: TP/FP/FN/FDR/recall must remain unknown. Report counts, coverage, retained and removed names, abundance, rho, S/C and score. The model was trained on synthetic NNLS outputs and is now applied to an ISTA export; this is exploratory transfer, not validated identity certainty.

Preserve input/model/code hashes, feature solver arrays, independent cached numerical review, candidate/molecular output tables and threshold bindings under results/real_ce29_joint_screening_v2. No source modifications or deletion. Save compact outputs to the authorized Git experiment branch; keep numeric arrays outside lightweight Git. Do not modify or stop the separately running new-composition development experiment. No progress polling unless requested; completion handling may review and save this one result.
