# Cached identity evidence: fixed development comparison

2026-09-13. User requests an actually useful screening indicator now and explicitly defers equal-strength, unequal-strength and strength-swap simulations. Reuse existing DEV, exposed old CHECK and exposed new-composition full/delete34-block fits. This is a new development experiment; all three datasets have previously exposed outcomes. No independent HOLD/FDR claim and no new observation, production fit, rho, profile, block/group definition, or reporting gate.

## Fixed indicators, before extraction or model outcomes

For held block h and molecular identity m (all aliases together), r=b_H-A_H*x_full; u=A_H,m*x_full,m; v=A_H*x_deleted,m-A_H*x_full+u. The original delta is ||r+u-v||²-||r||². Define d_h=||u||²+2r·u-u·v: average effect of deleting the identity before/after the other coefficients redistribute. delta-d_h is the complementary redistribution term. These are exact fixed-prediction arithmetic attributions, not unique causal or physical identity proofs.

Keep four observable features: T=(sum delta-max(0,max delta))/||b||²; D=sum d_h/||b||²; Q=asinh(sum d_h/(sum||u||²+tau)), tau=128*eps64*max(A.shape)*||b||²; K=mean(t)²/mean(t²), t being full_x summed over all aliases in blocks with both training and held nominal support. K=0 with an explicit uninformative flag if fewer than2 supported blocks or all t=0. D-information flag is sum||u||²>0. Flags are retained as diagnostics, not exclusion gates or fitted features. No clipping or label-selected offsets. The numerical denominator is not a new observation threshold. No hard rho0/D<=0/T<=0 deletion.

## Finite models and decision rule

Reference is the frozen V2 five-column [a,a²,r,s,c] model with existing DEV normalization and thresholds. Fit exactly three new models on the same174 reported DEV identities: TRIM adds T; DIRECT adds D,Q; JOINT adds T,D,Q,K. New feature means/population standard deviations are from reported DEV only (zero scale replaced by1). Existing five-column normalization is unchanged. Logistic sum loss plus .5*sum(coefficients²), unpenalized intercept; coefficients except a/a² constrained nonnegative. One L-BFGS-B from zeros per arm, maxiter2000, gtol1e-8, ftol2.220446049250313e-9, C1, no retry/search. Preserve optimizer diagnostics, objective, coefficients and every arm, including failures.

Apply the original complete-tie DEV95%-recall, DEV<=5%-FDP and DEV<=1%-FDP threshold policies separately to each arm. The single candidate for downstream use is chosen ONLY by DEV5%: maximum TP, then minimum FP, then fewer added features, then fixed arm order TRIM/DIRECT/JOINT. Freeze and successfully push the model/threshold/winner record before new CHECK scores. Baseline and all three arms are then reported on both exposed CHECKs without using either CHECK to choose a model or threshold. No further feature, coefficient or threshold revision follows weak results.

Primary actual-refit selection is the winner's fixed DEV5% set; report the high-recall and DEV1% sets as secondary screening only. This differs prospectively from the previous V2 high-recall-only refit and is never relabeled as that old endpoint. Refit the chosen fixed set on each CHECK once with unchanged NNLS/all-alias reporting >.001, regardless of its CHECK labels; an absent/empty DEV threshold produces an explicit no-fit record. If exact selected names and source/solver/runtime match a preserved fit, reuse that exact fit with provenance instead of duplicate solving. Keep all125 truths in recall, and split first-solver, filtering and refit losses. Report final concrete identities; groups never count as individual TP.

Useful improvement requires observed FDP<=5% on both CHECKs and recall no lower than the reference fixedDEV5% screen on each, with at least one recall increase; final refit counts must support the claim. Stronger target is <=5% with >=95% recall on both. Also retain the original1%/80% target as unresolved unless actually demonstrated at a separately identified endpoint. Training success alone does not meet any target. Cross-case comparisons remain development evidence, not a statistically controlled FDR estimate.

## Sources, execution and retention

Allowed sources are exactly the previously bound physical_block_prediction_v1 design/block_manifest/prepared DEV_TRAIN and CHECK inputs/records/cases34block NPZ+JSON/features/completions/reviews; physical_block_score_correction_v2 frozen model and new_composition_case prepared portable/block_manifest, nnls_rho records, physical_features34blocks/features/completion/reviews; original full prepared_arrays for the bound DEV/oldCHECK/new composition; existing physical_block_monotone_score matrix/count/threshold helpers, refit_screened_nnls and its unchanged baseline/KKT dependencies. Old files stay untouched. No realCE29 application or heavy archive scan is part of this task.

New files: analysis/cached_identity_evidence.py, analysis/run_cached_identity_evidence.py, analysis/review_cached_identity_evidence.py, focused tests and outputs under results/cached_identity_evidence_v1 (transfer archives in the corresponding _transfer folder). Save/hash exact inputs, implementation and runtime before computation. Independently audit cached feature arithmetic, all model/count/membership results and final arrays without refitting for review. Complete each newly learned dataset's cache review and retained backup, commit and successfully push that specific case before a downstream refit or the next case. Preserve all outputs, full records, final arrays, source/model/threshold bindings and block results. No cleanup/deletion. If local runtime differs from earlier fits, bind it explicitly and do not claim exact old-runtime reproduction; prefer original remote runtime for new final fits when available.

After the finite comparison and declared final refits, write results (including failure), update CURRENT_STATE/NEXT_TASK, commit/push and STOP. User's broad scientific objective is not marked solved unless actual evidence warrants it. The postponed intensity family is not launched.
