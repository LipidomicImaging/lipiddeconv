# Score mechanism correction and different true composition V2

2026-09-13. User explicitly requests repairing the identified scoring weakness, explaining the other seven false identities, and testing a different lipid composition. This is a new bounded development experiment; prior V1 code, results, thresholds, gates and stopped CAL/HOLD protocol remain unchanged.

## Mechanism and fixed correction

The old unconstrained rho-square term increases scores near the rho=0 floor. Correct that structural weakness, not the observed CHECK labels: one logistic model on [a,a^2,r,s,c], where a and r are the prior DEV-standardized log10 abundance/rho and s,c the prior DEV-standardized physical S,C. Remove rho-square, abundance*rho and zero-indicator terms. Constrain coefficients of r,s,c nonnegative; abundance terms and intercept remain free. Sum logistic loss plus 0.5*sum(coef^2), C=1, no intercept penalty, scipy L-BFGS-B maxiter2000/gtol1e-8, one fit from zero, no hyperparameter or feature search. Save convergence and projected-gradient checks. Existing rho floors, actual rho computation, observations, alias/report gates, physical blocks and NNLS remain unchanged. Monotonic scoring is not a correctness probability or guaranteed FDR.

Train once on the original 174 reported DEV identities using cached V1 features; preserve all 125 truths in recall denominators. Freeze coefficients and DEV rules before new-composition outcomes. Main cut: highest complete-tie score retaining at least95% of125 truths. Secondary cut: maximumTP under observed FDP<=5%, then minimumFP, then highest threshold. Also report the same <=1% descriptive rule. All cuts come only from DEV and transfer unchanged. Preserve absence of a feasible cut. Old exposed CHECK is a descriptive before/after development check, never threshold input. For matched old-model comparisons apply these same DEV policies to the sealed old model, explicitly recording model changes and policy.

## Sources and different composition

Reuse only existing prepared HOLD1 input from /root/identity_confidence_joint_validation/results/cases/HOLD1. Assign it the NEW_COMPOSITION_CHECK development role in a separate output directory; retire its unused independent-HOLD eligibility for future claims. Do not execute or bypass the stopped old runner gate or alter its records. The source has125 true molecular identities disjoint from the old CAL/DEV truth set and full391 nominal candidates. Keep its existing CV5% physical-ion perturbation, seed7501, spatial maps, relative abundances and global scale; no observation regeneration or seed/identity selection. This changes true identity composition and perturbation together; it is not a controlled equal-strength abundance challenge or an isolated causal test of composition alone.

Expected source hashes: prepared_arrays.npz 48a517e0f77568f7811ff8c711ab0223174ca8d8ad7053d20c5f7b7c6f52fbe7; input.json c316810365fdbd05151fa4edbafdb0195c0a1a85916d066be2b9d9e00cb603e2. Validate remaining input/metadata/nominal array/candidate order against existing preparation and V1 blocks. Existing prepared arrays must have no learned case result; no truth arrays enter inference features.

Allowed existing sources: physical_block_prediction_v1 model/prepared/blocks/results and its explicitly bound DEV/CHECK full arrays; identity_confidence_joint_validation preparation seal/records, original model, CAL1 and HOLD1 prepared inputs; corresponding original NNLS/rho/physical-block/refit/record helpers. No heavy repository or old-version scan. New files: analysis/physical_block_monotone_score.py; analysis/run_new_composition_development.py; necessary focused tests/review scripts; this protocol. New results only results/physical_block_score_correction_v2 and its transfer folder. Old unrelated work is excluded from commits.

## Execution and retained endpoints

1. Save code/protocol/source/runtime bindings and commit/push before the one DEV fit. Verify monotonicity including rho=0, objective gradient, finite outputs and label-independent prediction.
2. Fit once, independently replay model/cuts from cached outputs; preserve/push this specific model before new-composition computation.
3. Check old CHECK using the frozen corrected score and report TP/FP/FN, removed and added true/false names. Do not claim this predicts the new composition.
4. New composition: reuse four-worker per-pixel full-library NNLS/KKT and unchanged foreground-mean rho; separately reuse34 physical blocks and all377 full/deletion contrasts (12852 fits). Up to two independent stages may run concurrently, at most8CPU workers total and BLAS1; 4h/stage budget. All15837 (or source-verified foreground count) pixels use original mask. Partial progress may be reported with exact completed membership and explicit provisional status; no partial outcome replaces final.
5. Cache-only review finite/source membership/hashes, candidate and molecular records, original alias gate, block losses/support/Delta/S/C, and all-truth counting. Download and preserve all final arrays/blocks and compact audit; commit/push this specific completed case before subsequent refit. No retraining or rho rerun for review.
6. Apply frozen corrected and old-model DEV cuts for comparison. Refit only corrected95% pool once with original solver/all aliases/observation gate. Record first-solver misses, screen losses and refit losses separately, all truths as denominator. If pool absent/empty record explicit no-refit reason, not an invented outcome. Preserve exact retained names, intensities and any newly false identities; do not adjust model or cut after failure.
7. Save/push final result and update CURRENT_STATE/NEXT_TASK completed, then stop. No deletion, automatic next composition or guarantee of real acquisition FDR.

## Seven-FP cache mechanism diagnosis

Use existing CHECK full/block/delete solutions without any new solve. True A_target/X_true may be used only in a separately labeled explanatory audit to decompose known generating mismatch by true identity, inspect nominal spectral overlap and coefficient redistribution on deletion, and quantify concentration of positive held-block gains. They never enter features, selection or model fitting. Report deterministic arithmetic separately from proposed mechanism; shared spectrum/projection is not a unique causal donor attribution. Retain the three zero-rho failures separately so all10 old false identities remain accounted for.
