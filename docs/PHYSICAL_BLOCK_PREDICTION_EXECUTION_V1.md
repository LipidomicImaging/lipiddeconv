# Physical-block prediction: frozen development execution V1

2026-09-13. The user approved the proposed strategy and enabled the remote host. Execute a bounded CPU development pilot followed by its declared downstream decision. Reuse completed observations; no new simulation, production ISTA, rho/profile, GPU, old CAL2/HOLD or deletion.

## Inputs and order

Remote source DEV is /root/small_mismatch_nnls_first/results; CHECK is /root/identity_confidence_joint_validation/results/cases/CAL1. Both are exposed and share relative abundance/spatial templates. Component source is /root/small_mismatch_nnls_first/components; baseline model is /root/identity_confidence_joint_validation/results/model.json. Validate exact prior prepared_arrays/metadata/molecular records/result hashes against local expected-source hashes before aggregate extraction. Read only A_solver, B and mask from prepared arrays; never A_target/X_true for features, fitting or selection. Read molecular labels only in the evaluator/model training after frozen feature computation.

Freeze code, runtime (NumPy2.1.3, SciPy1.15.3, sklearn1.6.1), all input hashes, original candidate order, physical blocks and foreground-mean A/b inputs; download/verify and successfully push before real fitting. Complete DEV -> cache review -> download/hash -> commit/push exact DEV -> handoff. Fit one augmented classifier after DEV preservation; seal coefficients and TRAIN thresholds and push before CHECK feature computation. Complete CHECK -> same review/preservation/Git -> bounded final decision/refit -> final review/preservation/Git. No automatic next observation or method retry.

## Physical blocks and numerical score

Use every exact-positive channel support of each complete physical-ion component, union overlapping supports, sort resulting channel groups by minimum original channel index. Preserve 34 disjoint blocks, 391 candidate columns and 377 first-occurrence molecular names. Shared channels and all isotopes stay together. Nominal inputs alone determine blocks/support. These blocks are not statistically independent replicates.

For each block, use the other rows to fit the full391 library and, separately, delete each molecular name's ALL aliases, retaining all other original columns in original order. Use existing scipy NNLS maxiter3910; zero-training-support columns are explicitly zero, not labeled false. Validate each float64 result by the existing KKT rule. No warm start from full-data X_hat, held-out coefficient refit, change of solver tolerance, column normalization or retry. Store full and all377 deletion coefficient vectors and raw squared training/held-out losses in float64. Freeze the algorithm's selected solution; KKT does not certify unique held-out predictions. Record this ambiguity limitation and support/rank diagnostics.

Delta[g,k] is deletion held-out squared error minus full-model held-out squared error. Feature S[g] = sum over ALL34 Delta / ||b||². Feature C[g] is the fraction of nominally supported, training-supported blocks with Delta > tau, where tau=128*eps64*max(M,N)*max(deletion_loss,full_loss,||b_held||²). No absolute signal floor is introduced; all-zero b is unsupported and fails the real-case contract. A block supports g when any of its aliases has positive nominal held-row support and some alias has positive training-row support. Save all per-block Delta, tau, support flags and effective counts; define C=0 with explicit unsupported flag if count=0. Preserve all molecular records; unsupported/ambiguous cases stay in all-truth denominators.

## Budget and restart

Each case has34 full+12818 deletion=12852 NNLS tasks, executed as34 sequentially indexed block units using four CPU workers, BLAS1 thread per worker. The first indexed DEV block is the actual retained timing unit; it is not discarded or rerun. Record its duration and projected total before dispatching the remaining33. Budget four wall-clock hours per case and at most2GiB block outputs; stop/pause with preserved output if exceeded, without changing settings. Resume exact matching completed blocks only; a failed numerical block records failure and stops this version's execution for technical review, not scientific failure. Checkpoint/block arrays and all final arrays are retained, excluded from lightweight Git. No95-million-solve per-pixel deletion expansion.

## One model and two thresholds

BASELINE original model is preserved. AUGMENTED is one L2 logistic model C1, lbfgs, max_iter2000,tol1e-8,no class weighting. Use the original six rho/abundance transforms and original TRAIN mean/scale, plus raw S and C standardized only on the174 original reported DEV identities. Use standard-deviation floor1e-12 for constant numerical features, record constants. Fit exactly once; write a fit reservation before training, and immutable model seal after training. No hyperparameter/feature search or spatial-donor additions.

For originally reported DEV identities, select t_pool as the highest complete-tie score threshold retaining at least95% of all125 truths (at least119 TP); if impossible record pool=None. Select t_final_raw as the threshold with maximumTP under nonempty observed FDP<=1%, then minimumFP, then highest threshold; if none, final=None. Effective final threshold=max(t_pool,t_final_raw) if both exist, otherwise final=None. Save and re-count raw/effective thresholds, all scores and membership. These are TRAIN development rules, not independent calibration or FDR guarantees. CHECK receives unchanged coefficients, normalization and thresholds; original raw reporting gate controls candidate eligibility, not feature computation. All377 molecular features are computed with all391 competitors.

## Final pipeline and valid futility

The new candidate pool is all CHECK originally reported names passing t_pool, including any previously discarded by the spatial version. Retain every original alias. Final identity reporting is the intersection of this pool, score>=effective final threshold, and the unchanged post-refit candidate foreground mean>0.001/any-alias rule. Scores always use frozen original full-library evidence; no rescoring with reduced-library rho or altered abundance. Full125truth denominator remains.

After CHECK preservation, count the truth membership of the effective-final-score-eligible set as a DEVELOPMENT RECALL UPPER BOUND. If fewer than100 truths are eligible, no hard-subset refit under this frozen final rule can reach80% recall. Preserve this proof and stop before an unnecessary full refit, explicitly stating it was not run and that this is frozen-pipeline recall futility, not measured refit failure or general impossibility. An excessive FP count alone is not a futility proof because refit could remove FP.

Otherwise execute once on the retained pool for all15837 foreground pixels using existing four-worker NNLS/KKT and full391-index output. Apply original gate and frozen final-score rule, then evaluate raw/pool/score-eligible/refit-reported/final stages. Preserve all391 candidate/377 molecular rows, solver misses versus screen/final-score/refit losses, exact changed identities, residual and provenance. Compare prior120/8 and119/8 as historical workflows, explicitly noting changed screening policy; do not label this a feature-only matched-threshold ablation. Always report strict1%/80% and secondary5%/80%, all-truth recall and retention. Group ambiguity is not an individual TP.

## Completion and scope

Independent cache review must replay saved predictions/losses/support/score/threshold/accounting and all relevant source/code/array hashes without NNLS, training or rho reruns. Download compact records and all new block/final arrays with matching hashes. Push each specific case before the next handoff; hashes alone are not backups. Update CURRENT_STATE/NEXT_TASK, push final development result and stop this version. Even a passing development result does not authorize reopening old HOLD; abundance-family/new CAL/HOLD and real known-mixture validation require their own prospective frozen contract. Main scientific objective remains unresolved until adequate final evidence exists.
