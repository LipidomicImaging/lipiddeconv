# Active goal: diagnose, develop, and independently test 1% / 80%

The user now explicitly requests continuing analysis and exploration until the target is reached. This supersedes the previous per-turn STOP scope; it does not authorize changing previous scientific results, disguising adaptive development as independent validation, or guaranteeing an unknown scientific outcome. Keep all attempts, their rationale, frozen implementations, failures and Git provenance.

The current physical scope remains the user-specified complete 391-candidate library with independent physical-ion multiplier CV5%, fixed per experiment, original whole-column normalization, full pixelwise CPU NNLS, frozen rho and report gate. No CE mismatch, peak deletion, pixel noise, missing-library challenge, new K ladder or GPU is introduced.

## Diagnosis before another method change

The completed auxiliary screen did not improve the existing six-feature rho/abundance logistic classifier. Cached analysis separates ranking capacity from calibration loss: each prior fold used only25–41 reported CAL identities. An exposed-label, fold-specific oracle could retain107TP/1FP (85.6% recall,0.926% FDP) under the existing rankings. Those oracle cutoffs are prohibited for inference; they are only a diagnostic showing that insufficient ranking and small-sample calibration are different problems. The oracle does not establish that a new calibration will succeed.

First test the simpler hypothesis: one shared classifier trained using the entire already-exposed development case, followed by larger independent case-level calibration, may improve the tradeoff without adding features. This is more directly motivated than another feature search. Use the same six features, regularization and optimizer; no new architecture/hyperparameters. The 20 auxiliary models remain negative records and are not included.

## Next bounded experiment: shared model with new CAL and HOLD observations

Train once on reported molecular identities of the already exposed `NNLS_FULL_LIBRARY_5PCT__CAL_R71_K125`. Freeze coefficients, TRAIN normalization and source hashes before new results. Generate two new CAL and two new HOLD cases, sequentially, with seeds7401/7402 and7501/7502 respectively. Use the existing fixed5% generator and original spatial maps/relative abundances. No per-identity scaling; the existing one global dataset scalar targets foreground median B norm0.6036783456802368.

CAL spatial sources: `SUPPORTED_MISMATCH__CAL_R71_K125` and `SUPPORTED_MISMATCH__CAL_R72_K125` in `/root/physical_identity_mainline_v1/results/cases`. HOLD spatial sources: `close_neighbor__HOLD_R71_K125` and `close_neighbor__HOLD_R72_K125` there. Only their full391-column X_true and foreground masks are read; old B, CE target spectra and omitted-candidate lists are not inference or generative inputs. These HOLD source containers hold the complete125-truth X maps despite their earlier missing-library task names. The new experiments use the complete391-column nominal library, with no omission. Original files and original labels are not changed.

The CAL and HOLD truth sets are disjoint (125each), using the frozen original identity pools; the same candidate universe is shared. The development training table can contain some HOLD identities as false allocations, so this is new-observation/new-perturbation validation with distinct true-identity pools, not a claim that all candidate names are unseen during training. Fresh perturbation draws are independent. Existing spatial templates and library were previously studied, so this is not independent tissue/library/real-data validation.

Run CAL1 first. If no CAL1 cutoff of the frozen shared score reaches FDP<=1%, recall>=80% and retention>=80%, preserve its negative result and do not spend resources on the remaining three solver runs for this version. Continue the broader goal by diagnosing that failure before proposing a new scientifically motivated method; do not tune this version's hyperparameters or substitute test-aware cutoffs. A failed version does not close the broader goal.

If CAL1 is feasible, run CAL2, then choose one common tied-score cutoff maximizing pooled CAL retention under pooled and each-case FDP<=1%; require each CAL case recall and TP retention>=80%. If no such common cutoff exists, stop this version before HOLD. Otherwise seal before any HOLD solve or outcome review. HOLD1/HOLD2 use this unchanged classifier/cutoff; each must have nonempty output, FDP<=1%, recall>=80%, retention>=80%, with pooled accounting also reported. After any completed case, review/download/hash/Git before starting the next. Final success requires both prospective HOLD cases; no early success from CAL or a lone HOLD. The error goal is evaluated with observed FDP and statistical limitations reported; a small pilot cannot guarantee population FDR<=1% on real MSI.

HOLD1 failure also stops the remaining HOLD2 run for this version; it cannot be hidden by pooled success. No automatic experiment sweep, feature/hyperparameter grid, seed selection or cherry-picked successful iteration. The prepared four-case contract is frozen before any solver run. Outcomes that become exposed cannot be reused as independent validation in a later version. Record source memberships, aliases, all truths, actually perturbed truths, solver misses, filter-induced misses, all candidate/molecular data, model and threshold seals, and finite/process checks. Large arrays remain in documented storage, compact records in Git. No cleanup is authorized by this new plan beyond existing separately audited rules; do not delete for this experiment.

## If new evidence is later needed

Consider measured fragment support and perturbation-sensitive allocation only after the above diagnostic test. DIA proteomic tools use fragment match/interference evidence alongside scores, e.g. [EncyclopeDIA](https://www.nature.com/articles/s41467-018-07454-w), but its chromatography and peptide decoy guarantees do not transfer to MSI. Resampling-based stability can also be studied, but [stability selection](https://arxiv.org/abs/0809.2932) requires its own assumptions for error bounds; bootstrap frequency is not automatically an identity probability. These are conditional research directions, not additional runs in this frozen four-case plan.
