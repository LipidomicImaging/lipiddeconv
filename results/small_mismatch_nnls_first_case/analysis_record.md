# 单组 NNLS + 5% 谱形失配：最终开发筛查

**结果：首组 developmental GO。** 预先规定的单组阈值选择规则保留 60 个真身份、0 个假身份；本次 FDP=0%，TP retention=48%，all-truth recall=48%。达到 1% / 40% 的继续线，未达到 60% 强成功线。这是首组开发可行性结果，不是独立 FDR 控制验证或真实实验验证。没有启动第二组。

## 同一整组数据的三个结果

| 阶段 | TP | FP | FN | 本次 FDP | TP retention | all-truth recall |
|---|---:|---:|---:|---:|---:|---:|
| 原始 NNLS 报告 | 125 | 49 | 0 | 28.16% | 100% | 100% |
| 原固定 rho >= 1e-3 | 2 | 0 | 123 | 0% | 1.6% | 1.6% |
| 冻结开发规则选择的阈值 | 60 | 0 | 65 | 0% | 48% | 48% |

原始 solver misses=0；最终未保留的 65 个真身份全部是 filter-induced true losses，比例 52%。125 个真身份均为全局 reportable truth，也全部实际受到新扰动，因此受扰动真身份的 retention 同为 60/125=48%。保留 60 个 distinct molecular identities。

阈值按预先冻结的完整同分曲线规则选择：经验 FDP<=1%、非空集合下最大 TP，并列取更少 FP，再取更高阈值。结果为 rho >= 7.855148751253491e-7。最高假身份 rho=6.474439543364030e-7；两者相差 1.3807092078894615e-7。保存了所有候选 q_star/q_deleted 和原 rho 公式。浮点差分尺度仅作描述，不代替优化器误差界，也没有事后加入数值筛选门槛。

固定 1e-3 的原结果完整保留；没有将正式旧阈值改写成这次开发阈值。全曲线与选点规则均在读取结果前冻结。根据同一组真值选择的最佳点仍需新数据独立检验，0/60 的经验错误数不能解释为已经保证真实 FDR<=1%。

## 本次输入与执行

只运行 NNLS_FULL_LIBRARY_5PCT__CAL_R71_K125，seed7301，完整391候选库、125个真身份、15837个前景像素。全部391候选的2621个fragment envelopes及391个precursor envelopes参与扰动；保持离子包络内部耦合。每个物理离子使用均值校正的lognormal独立倍率，归一化前均值1、CV=5%；一个实验内所有pixel共用同一张扰动谱。随后保留原列L2范数，所有空间真值只乘一个共同dataset scalar。无dropout、缺库、CE偏移或pixel噪声。

这是用户指定的小幅同条件谱形误差假设，不是由实验重复估出的误差分布。沿用已曝光R71空间图和相对丰度，仅用于开发筛查。名义库、production代码、rho定义及同名聚合不变；CPU NNLS全391列逐像素求解。GPU训练未执行。北京时间16:10:45启动，16:33:13完成。输入先封存并推送af4b319，完成后才分析结果。

## 审查、数据与数值边界

原主机独立缓存审查 PASS；333个最终文件下载哈希全部匹配。本地再次核验来源/设计/数组、全部391候选与全部分子记录、174个rho结果、64个NNLS块、完整阈值曲线、各项分母和GO判定；结果一致。没有重跑求解或rho。大数组、源容器及所有块在远程和本地保留，Git只保存紧凑记录与分析。

重建相对残差约0.408%。冻结runner的分母以float32汇总大数组，导致平台相关的最后汇总差异：原主机0.004083841917479932，本地相同dtype公式0.004084216164627303；本地float64分母参考值0.004083482886460519。原主机精确检查通过；本地使用绑定同一结果/数组哈希的原主机检查记录该标量，同时独立验证全部数组前向关系和身份指标。没有放宽任何身份阈值，也没有修改存储结果。本地审查文件明确列出这项可移植性差异。

局部10000像素预览的125TP/53FP已保留为历史快照；整图均值下最终为125TP/49FP，不能混用两者。所有非入选真身份仍在原分母内。

## 结论与停止点

这次结果支持：在本次全库5%小幅谱形失配假设下，原rho排序包含有用的身份区分信息，能够在一个开发case中筛出达到目标效用的子集。固定1e-3阈值过于保守；此前CE端点/不确定性实验的失败不能代替对本次假设的判断。

当前尚未证明该阈值跨case稳定、能控制独立实验FDR，也尚未完成真实DIA MSI验证。下一步如继续，应预先封存校准/独立验证合同，保持本次生成与筛选规则，不在已曝光结果上反复改模型。本轮按单组合同停止，不自动开展新case、缺库、GPU或方法变体。
