# 首组 CAL：当前筛选判据未通过，停止剩余五组

2026-09-12，北京时间。用户要求先检查已完成的第一组，并在无继续价值时停止后续 GPU 计算。本次为单组开发止损检查，不是正式 EVAL 结果，也不声明主线不可能。

第一组 `SUPPORTED_MISMATCH__CAL_R71_K125` 是完整候选库下的受控谱形失配，没有缺库。原生产拟合正常完成，原始结果已在 `382ee2091773d93d027faecd93b27fde5f22e4a6` 推送后交接。CPU 筛查协议在结果产生前以 `edbd272` 推送。原六组设计、MODEL uncertainty、完整/删除模型、数值门槛、报告门槛及分母均未改变；其余五组没有启动。

| 指标 | 原始 solver | 筛选后 |
|---|---:|---:|
| TP | 123 | 23 |
| FP | 109 | 0 |
| FN（125 个真身份） | 2 | 102 |
| all-truth / reportable-truth recall | 98.4% | 18.4% |
| TP retention | — | 18.70%（23/123） |
| 实际受扰动真身份保留数 | 26/26 | 2/26 |
| 实际受扰动真身份 retention | — | 7.69% |

solver 自身漏掉2个真身份；筛选又额外丢掉100个已找到的真身份，其中24个属于实际受扰动子集。未入选身份没有从 recall 或 retention 分母移除。受扰动真身份是仅含真值的子集，因此不为它虚构 FP/FDP。

按冻结阈值事件规则检查完整曲线：满足单组经验 FDP≤1% 时，最多保留23个TP、0个FP。更关键的是，即使去掉 FDP 限制，当前完整模型接受条件加删除下界判据也最多保留23个TP，受扰动子集最多2个TP。没有任何被检查的阈值区间能达到全体至少50个TP及受扰动子集至少11个TP的40%要求。本次不是通过继续放松假发现要求就能解决的权衡。

单组开发选点 epsilon=`0.0016178842305511923`，不是正式 CAL seal。0/23 是本组经验 FDP=0，不是独立验证的总体 FDR≤1% 保证。原 EVAL 未读取、未运行，原六组 pooled 风险合同未被改写。

状态：`EARLY_STOP_CAL_FUTILITY`。232个已报告身份全部完成，240个 LP 数值证明复核通过；最大数值 gap=`4.393614826436775e-07`，没有未解决的数值结果。完整模型界为 `[0, 0.0015248914687511666]`；其中上界来自合法物理模型的一个可行解，下界来自凸松弛，不能把二者之差当成数值求解误差。

本次没有保留的身份也不能直接称为“已经证明在真实物理模型中可替代”。删除检验使用更宽的凸松弛集合；该集合中的替代解释未必满足原物理模型。计算保守性与实际不可辨识性尚未区分。这个结果关闭当前判据的本次开发运行，不证明任何高置信身份策略都不可能。此外，本实验仍是已封存的 CE endpoint 受控测试，不能冒充正确 CE 谱库下真实残余失配范围的验证。

保全与停止：479个导出文件已逐一下载/hash核对，数值证明 NPZ 在本地与远端保留，紧凑记录、完整曲线、来源哈希、独立审查与执行记录进入 Git。原模型、数组、训练历史及所有准备好的输入均保留；未删除文件。远端停止记录确认其余五组均无 launch、reservation、training_complete 或 ACK，主线 runner 无活动进程，GPU 为0%/0MiB。后续不自动恢复协调器，不补跑缺库或 EVAL，不在已曝光第一组上调参。

机器结果：`screening_report.json`、`threshold_curve.json`、`independent_accounting_review.json`、`local_transfer_review.json`、`execution/mainline_first_cal_early_stop.json`。原始与筛选分子记录、全/删除证明绑定在 `scores/SUPPORTED_MISMATCH__CAL_R71_K125/`。
