# Shared-model validation: prepared before new results

The user explicitly authorized the exact seven-file package upload and sequential CPU execution on westc:55786. The historical automatic-review upload rejection has been resolved by that authorization; no credential is stored in this record.

One fixed six-feature model was trained only on the old, exposed 174 reported identities, then sealed. Four new full-library 5% observations are prepared; each contains 125 reportable, actually perturbed true molecular identities. CAL/HOLD true pools are disjoint. Spatial sources are unchanged apart from the existing one global dataset scalar. This is fresh perturbation validation within a previously studied library/spatial design, not real MSI validation.

Remote cached input review passed array hashes, forward equations, foreground membership, one-scalar source-X equivalence, model/source seals and absence of learned case results. All 17 compact input/model records were downloaded with matching hashes. Large prepared arrays remain at `/root/identity_confidence_joint_validation/results/cases/<case>/prepared_arrays.npz` and are not deleted or placed in lightweight Git.

Compile, help and five analytic tests passed. Additional handoff tests enforce CAL1 failure stopping CAL2, reviewed Git handoff before the next case, and CAL sealing before HOLD. Source and protocol are unchanged since pre-execution commit 96de863.

Next: after pushing this prepared snapshot, run only CPU CAL1. Its outcome must be independently reviewed, downloaded and pushed before any CAL2 run. A feasibility failure stops this version's remaining solves, not the broader scientific objective. No GPU, missing-library challenge, CE perturbation, deletion or threshold/model modification is introduced.
