# Two-stage development inputs, models and candidate pools frozen

2026-09-13. Protocol/implementation/input seal was successfully pushed as `4347b0d` before feature extraction or the one augmented classifier fit. Design SHA256: `2396794e23d148b3ea8210d5bcfff7b23448580f32b694e2c09a97f5edbba795`. All 27 analytic/integration tests passed. Both original cases' prepared arrays, learned arrays, candidate/molecular records and the reused baseline model matched their original result hashes.

One fixed eight-feature classifier was fitted on the 174 originally reported DEV_TRAIN identities. The six original rho/abundance features are augmented with log single- and two-donor spatial residuals. The baseline model was reused without fitting. Both development TRAIN candidate-pool cutoffs select 117 true and 6 false identities on TRAIN (4.878% observed FDP); these are in-sample development cutoffs, not independent calibration. The baseline cutoff is 0.6710384248834387 and the augmented cutoff is 0.632840346532133.

Applying the frozen models/cutoffs to the already-exposed CAL1 observables produces 127 baseline and 128 augmented candidate identities/columns for the two planned reduced-library refits. No CAL1 outcome-dependent threshold was chosen. Feature extraction accessed cached X_hat and names; selection received observable fields only. Model and selection seals preserve all scores, memberships and training hashes.

The cached scalar/matrix prediction review reproduces scores with maximum absolute discrepancy 2.22e-16, verifies TRAIN tied-score cutoff optimality, and verifies CHECK selection membership. No model was fitted for review. The CHECK feature Gram/direct residual discrepancy is 1.14e-15, with no near-collinear fallback contexts. Full source and runtime bindings are preserved; this local runtime is NumPy1.25.2/SciPy1.11.3/scikit-learn1.2.2, separate from the original remote run. No old output was recalculated or rewritten.

Next push these exact model/candidate-pool seals, then BASELINE refit and cached review/Git preservation, followed by AUGMENTED under the same protocol. Final qualitative outcomes are pending at this seal. Both cases have been exposed and share the original identity/spatial context; this is development evidence only. Original CAL1 NO-GO and the stopped CAL2/HOLD runs remain unchanged. No new observation, GPU or cleanup.
