# Physical-block predictive evidence: prepared before execution

2026-09-13. User approved the strategy and enabled the existing westc:55786 host. Authenticated transport is available; no credential is stored in the repository. Existing DEV, exposed CHECK and component arrays remain on that host.

Local and remote: **36 analytic tests passed**, covering complete physical-envelope closure, shared channels, all-alias deletion, held-out-observation exclusion from coefficient fitting, original KKT/cache replay, unsupported columns, whole-truth denominators, tied-score thresholds, empty final sets, scale invariance, reported-only inference with null unreported rho, and independent scalar model/threshold replay. Final remote run:36 passed in0.41s. Scientific NumPy2.1.3/SciPy1.15.3/sklearn1.6.1 are unchanged. A missing test runner was installed into the isolated project test_dependencies directory; it does not replace numerical packages.

All11 original source hashes matched. The full physical-component/overlap closure has34 blocks,391 candidate columns and377 molecular names. Exact A_solver/B/mask inputs were reduced only to frozen foreground-mean A/b; no A_target or X_true entered preparation/features. Both source observations were already exposed; no independent FDR claim.

Design SHA256: `8b7d878954ae76cec513cd956bcf56fcab8e2339c55fbff64ae5c863d168c56a`. All11 scientific code/test/protocol files are hash-bound. The downloaded preparation archive SHA256 is `207adfe6a7aa324bbf3ad2b8661f56f9cb7e437288bbc21008b4a24eb50b8863` and matches the remote archive; extracted aggregates, records, model and manifest pass local source/seal validation.

Before actual DEV solving, push this exact code/input snapshot and record its handoff. The retained first indexed block is the runtime estimate. Complete/review/download/push DEV before the single augmented fit; seal/review/push that model before CHECK. No new observation, old CAL2/HOLD, GPU, rho/profile/ISTA or cleanup is queued. Existing source arrays and future block arrays remain preserved on both documented storage paths as applicable.
